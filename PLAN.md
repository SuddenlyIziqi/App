# 🧊 冰箱食物管理应用 - 完整设计方案

## Context

个人/小家庭场景，需要一个免费的应用来管理冰箱中的食物，解决食物过期浪费、忘记库存等问题。核心诉求是**快速录入** + **智能管理** + **到期提醒**。

---

## 一、技术选型建议

### 推荐方案：Web App (PWA) + React + Vite

| 对比维度 | 微信小程序 | Web App (PWA) ✅ | Flutter |
|---------|-----------|------------------|---------|
| 费用 | 免费 | 免费 | 免费 |
| 本地存储 | 有限 | IndexedDB 大容量 | SQLite |
| 语音识别 | 微信API | Web Speech API | 需第三方 |
| 图像识别 | 微信AI | TensorFlow.js / 浏览器AI | TensorFlow Lite |
| 通知推送 | 模板消息 | Notification API | 本地通知 |
| 跨平台 | 仅微信 | 全平台浏览器 | iOS/Android |
| 开发效率 | 中 | 高 | 中 |
| 离线使用 | 有限 | Service Worker 完整离线 | 原生支持 |

**选择理由**：PWA 方案零成本、跨平台、支持现代浏览器 API（语音/图像识别）、可添加到手机桌面像原生 App 一样使用。

### 设计决策
- **界面语言**：中英双语支持（i18n），默认中文
- **开发节奏**：一步到位，完成所有功能后发布
- **视觉风格**：清新可爱风 — 圆角卡片、柔和色彩、食物 emoji 图标、亲切温馨

### 技术栈
- **框架**: React 18 + TypeScript + Vite
- **UI**: Tailwind CSS + Headless UI（轻量美观）
- **路由**: React Router v6
- **状态管理**: Zustand（轻量级）
- **本地存储**: IndexedDB（通过 Dexie.js 封装）
- **语音识别**: Web Speech API（浏览器原生）
- **图像识别**: TensorFlow.js + MobileNet（本地 AI，无需服务器）
- **条码识别**: QuaggaJS / ZXing
- **通知**: Notification API + 本地定时器
- **PWA**: Vite PWA Plugin
- **国际化**: react-i18next（中英双语）
- **图表**: Recharts（统计报表）

---

## 二、完整功能清单

### 📝 1. 食物录入（多种方式）

| 录入方式 | 说明 | 优先级 |
|---------|------|--------|
| 文字快速录入 | 手动输入食物名称，支持自动补全 | P0 |
| 语音转文字 | 调用 Web Speech API，说出食物名称自动填入 | P0 |
| 拍照/图片识别 | 拍照后 AI 识别食物种类（TensorFlow.js） | P1 |
| 条码扫描 | 扫描食品包装条形码，自动匹配食物信息 | P2 |
| 常用食物快选 | 预设常见食物列表，一键选择录入 | P0 |
| 批量录入 | 一次录入多个食物（如购物回来统一录入） | P1 |
| 语音快捷录入 | 说出"牛奶，冷藏，7天"自动解析录入 | P2 |

### 📂 2. 分类管理

**冰箱区域划分**：
- 🧊 冷藏室（0-4°C）
- ❄️ 冷冻室（-18°C）
- 🌡️ 保鲜层/果蔬盒
- 🚪 门架
- 自定义区域

**食物大类（预设）**：
- 🥬 蔬菜类
- 🍎 水果类
- 🥩 肉类
- 🦐 海鲜类
- 🥛 乳制品类
- 🥤 饮品/饮料
- 🧂 调味品/酱料
- 🍱 熟食/剩菜
- 🧊 冷冻食品
- 🍞 面食/烘焙
- 🥚 蛋类
- 🫙 罐头/腌制品
- 自定义分类

**标签系统**：
- 自定义标签（如"有机"、"自制"、"打折购入"）
- 多标签筛选

### ⏰ 3. 保质期管理

- **预设保质期数据库**：内置 200+ 常见食物的默认保质期（冷藏/冷冻分别设置）
- **自定义保质期**：用户可手动设定任意食物的保质期
- **智能计算**：录入时根据录入日期 + 保质期自动计算到期日
- **状态标识**：
  - 🟢 新鲜（剩余 > 50%）
  - 🟡 正常（剩余 20%-50%）
  - 🟠 临期（剩余 < 20%）
  - 🔴 已过期
- **保质期学习**：记录用户实际丢弃时间，优化保质期建议

### 🔔 4. 到期提醒

- **多级提醒策略**：
  - 到期前 3 天：温和提醒
  - 到期前 1 天：紧急提醒
  - 到期当天：立即处理提醒
  - 已过期：警告提醒
- **自定义提醒规则**：可按食物类别设置不同提醒时间
- **浏览器通知推送**：打开页面时自动检查并推送
- **首页醒目展示**：过期/临期食物在首页突出显示

### 🏠 5. 首页/仪表盘

- **冰箱概览卡片**：
  - 总食物数量
  - 今日临期数量
  - 已过期数量
  - 本周需消耗数量
- **紧急提醒区**：过期和即将过期的食物列表
- **快速操作区**：快速录入、快速查看临期食物
- **新鲜度环形图**：可视化展示各类食物新鲜度
- **最近操作记录**：显示最近的食物操作

### 🔍 6. 搜索与筛选

- 食物名称模糊搜索（支持拼音首字母）
- 按分类筛选
- 按冰箱区域筛选
- 按状态筛选（新鲜/临期/过期）
- 按标签筛选
- 排序：按到期时间、录入时间、名称

### 📊 7. 统计报表

- **库存统计**：各分类食物数量分布饼图
- **消耗趋势**：折线图展示每周/月消耗量
- **浪费分析**：
  - 过期丢弃食物统计
  - 浪费金额估算
  - 浪费率趋势
- **保鲜排行**：哪些食物总是最先过期
- **月度报告**：每月食物管理总结

### 📋 8. 操作记录

- 放入（记录时间、位置）
- 取出/消耗（标记已使用）
- 丢弃（记录丢弃原因，用于统计）
- 转移（从一个区域移到另一个区域）
- 修改（修改数量、保质期等信息）

### 🍳 9. 智能推荐（P2 高级功能）

- 根据即将过期食物推荐搭配菜谱
- 根据库存推荐购物清单
- 食物搭配建议（哪些食物不宜放在一起存放）

### ⚙️ 10. 系统功能

- **数据备份/恢复**：导出 JSON/CSV，支持导入恢复
- **深色模式**：跟随系统或手动切换
- **PWA 安装**：添加到桌面，全屏运行
- **离线使用**：Service Worker 完整离线支持
- **数据清理**：一键清理所有过期记录

---

## 三、数据模型设计

```typescript
// 食物条目
interface FoodItem {
  id: string;              // 唯一ID
  name: string;            // 食物名称
  categoryId: string;      // 分类ID
  areaId: string;          // 存放区域ID
  quantity: number;        // 数量
  unit: string;            // 单位（个/袋/盒/kg等）
  purchaseDate: Date;      // 购入/放入日期
  shelfLifeDays: number;   // 保质期（天）
  expiryDate: Date;        // 到期日期
  status: 'fresh' | 'normal' | 'expiring' | 'expired'; // 新鲜度状态
  tags: string[];          // 自定义标签
  image?: string;          // 食物图片（Base64）
  barcode?: string;        // 条形码
  notes?: string;          // 备注
  createdAt: Date;         // 创建时间
  updatedAt: Date;         // 更新时间
}

// 分类
interface Category {
  id: string;
  name: string;            // 分类名称
  icon: string;            // 图标（emoji）
  color: string;           // 颜色标识
  defaultShelfLife: {      // 默认保质期
   冷藏: number;
    冷冻: number;
    保鲜: number;
  };
  sortOrder: number;       // 排序
  isPreset: boolean;       // 是否预设分类
}

// 冰箱区域
interface FridgeArea {
  id: string;
  name: string;
  icon: string;
  temperature?: number;    // 建议温度
  sortOrder: number;
  isPreset: boolean;
}

// 操作记录
interface OperationLog {
  id: string;
  foodId: string;          // 关联食物ID
  type: 'add' | 'consume' | 'discard' | 'transfer' | 'modify';
  fromAreaId?: string;     // 转移前的区域
  toAreaId?: string;       // 转移后的区域
  quantity: number;        // 操作数量
  reason?: string;         // 丢弃原因等
  note?: string;
  createdAt: Date;
}

// 保质期预设
interface ShelfLifePreset {
  id: string;
  foodName: string;        // 食物名称
  categoryId: string;      // 分类ID
  shelfLife: {
    冷藏: number;          // 天数
    冷冻: number;
    保鲜: number;
    常温: number;
  };
  source: 'preset' | 'user'; // 来源
}

// 提醒设置
interface ReminderSettings {
  expiringDays: number[];  // 提醒时间点，如 [3, 1, 0]
  notificationEnabled: boolean;
  quietHoursStart?: string; // 免打扰开始时间
  quietHoursEnd?: string;   // 免打扰结束时间
}

// 用户设置
interface UserSettings {
  theme: 'light' | 'dark' | 'system';
  language: 'zh-CN' | 'en';
  reminder: ReminderSettings;
  defaultArea: string;     // 默认录入区域
  defaultUnit: string;     // 默认单位
}
```

---

## 四、页面结构规划

```
📱 应用结构
├── 🏠 首页 (Dashboard)
│   ├── 冰箱概览卡片（总数/临期/过期）
│   ├── 紧急提醒区（过期+临期食物列表）
│   ├── 新鲜度可视化
│   └── 快速操作入口
│
├── 📦 食物列表 (FoodList)
│   ├── 全部食物（网格/列表视图切换）
│   ├── 搜索栏 + 筛选器
│   ├── 分类标签页
│   └── 食物卡片（显示名称/到期/状态）
│
├── ➕ 录入食物 (FoodAdd)
│   ├── 快速录入（文字+语音）
│   ├── 拍照识别
│   ├── 条码扫描
│   ├── 常用食物快选
│   └── 批量录入模式
│
├── 📊 统计 (Statistics)
│   ├── 库存分布图
│   ├── 消耗趋势图
│   ├── 浪费分析
│   └── 月度报告
│
├── ⚙️ 设置 (Settings)
│   ├── 冰箱区域管理
│   ├── 分类管理
│   ├── 保质期预设管理
│   ├── 提醒设置
│   ├── 数据备份/恢复
│   ├── 主题设置
│   └── 关于
│
└── 📋 操作记录 (History)
    ├── 操作时间线
    └── 筛选/搜索
```

---

## 五、实施路线图

### 🚀 阶段一：项目基础 + 数据层 — 预计 1-2 天

| 任务 | 说明 |
|------|------|
| Task 1 | 项目初始化：Vite + React + TS + Tailwind + PWA + i18n 配置 |
| Task 2 | 数据层：Dexie.js + IndexedDB，定义数据模型，实现 CRUD |
| Task 3 | 全局布局：底部导航栏 + 页面路由 + 清新可爱主题配置 |
| Task 4 | 国际化框架：中英文双语资源文件 + 语言切换 |

### 🍎 阶段二：核心录入 + 列表 — 预计 2-3 天

| 任务 | 说明 |
|------|------|
| Task 5 | 食物录入页：文字录入 + 自动补全 + 常用食物快选 + 保质期自动计算 |
| Task 6 | 语音转文字录入：Web Speech API 集成 |
| Task 7 | 拍照食物识别：TensorFlow.js + MobileNet |
| Task 8 | 条码扫描识别：QuaggaJS 集成 |
| Task 9 | 批量录入模式 |
| Task 10 | 食物列表页：网格/列表视图 + 搜索 + 多维筛选 + 排序 |
| Task 11 | 食物详情页：查看/编辑/删除 + 操作记录 |

### 🏠 阶段三：首页 + 分类 + 提醒 — 预计 2 天

| 任务 | 说明 |
|------|------|
| Task 12 | 首页仪表盘：概览卡片 + 紧急提醒 + 新鲜度可视化 + 快速操作 |
| Task 13 | 分类管理：预设分类 + 冰箱区域管理 + 自定义标签 |
| Task 14 | 保质期预设数据库：200+ 常见食物保质期数据 |
| Task 15 | 到期提醒：本地通知推送 + 状态自动更新 + 多级提醒策略 |

### 📊 阶段四：统计 + 记录 + 智能 — 预计 2 天

| 任务 | 说明 |
|------|------|
| Task 16 | 操作记录页：时间线展示 + 筛选 |
| Task 17 | 统计报表：库存分布 + 消耗趋势 + 浪费分析 + 月度报告 |
| Task 18 | 智能菜谱推荐（基于临期食物） |

### ⚙️ 阶段五：系统功能 + 打磨 — 预计 2 天

| 任务 | 说明 |
|------|------|
| Task 19 | 设置页：区域管理 + 分类管理 + 提醒设置 + 主题 + 语言 |
| Task 20 | 数据备份/恢复：JSON/CSV 导出导入 |
| Task 21 | 深色模式 |
| Task 22 | PWA 离线支持优化 |
| Task 23 | 动画效果 + 交互细节打磨 |
| Task 24 | 全面测试 + Bug 修复 |

---

## 六、验证方案

1. **功能验证**：逐页测试每个功能的录入、展示、筛选、提醒
2. **离线验证**：断网后确认 PWA 可正常使用所有功能
3. **数据验证**：导出 JSON → 清除数据 → 导入恢复 → 确认数据完整
4. **通知验证**：设置临期食物 → 等待提醒触发 → 确认通知推送
5. **语音验证**：测试语音转文字在不同浏览器的兼容性
6. **AI 验证**：拍照识别常见食物的准确率
